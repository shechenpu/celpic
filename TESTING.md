# CelPic 检查记录

日期：2026-09-05

## 自动检查

- Node.js 24 环境，`node --check server.js` 和 `node --check public/app.js` 通过。
- `node --test tests/integration.test.js`：17 项通过，0 项失败。
- `tests/browser-smoke.cjs`：使用本机 Chrome 无头运行，通过。
- 所有测试均使用临时数据库与存储目录，不使用项目内的真实账号数据。

## 图片范围与权限回归

- 管理员个人图库只返回自己的图片，全站图库返回所有图片及上传者；两个范围的数量与大小统计分别校验。
- 普通用户请求全站列表、统计或删除均返回 403；未登录请求返回 401，无效范围返回 400。
- 个人范围批量删除混入其他用户图片时整批拒绝，文件保留；普通用户可删除自己的图片，管理员可在全站范围删除他人图片。

## 已检查的浏览器流程

- 首次创建管理员：两次密码不同显示行内错误，不能创建；改成一致后成功。
- 管理员创建普通用户：相同确认流程，错误后不会出现多余账号。
- 开放注册：注册页两次密码校验，正确填写后成功。
- 单张长图上传，以及五张成功 / 一张伪造图片失败的混合上传；逐文件状态、数量、全选统计正确。
- 上传期间切换页面再返回，上传状态和结果恢复显示。
- 上传、我的图片与管理员图片管理是独立页面；普通用户不显示管理菜单，两个图库统计均位于搜索工具栏上方。
- 管理员与普通用户分别上传后，各自个人图库只显示本人图片，全站图库显示两者及上传者；切换范围清空选择，延迟返回的全站请求不会覆盖个人图库。
- 大图预览内复制 HTML 后，通过真实剪贴板与 elementFromPoint 校验提示内容和最上层命中，截图确认右下角提示不再被遮罩压暗。
- 每张成功上传卡片只有一个链接框；直链、HTML、Markdown 图标切换实际内容并复制，通过真实剪贴板校验（兼容 Windows 换行）。每张卡片独立保留格式，切换页面后仍保留。
- 图库卡片仅含三种复制图标与删除图标，无链接文本框；复制不会误开预览。单张删除取消后数据不变，确认删除只移除对应图片并保留其他选择；批量复制、批量删除及上传结果清理通过。
- 文件名中的方括号、尖括号和 & 在 HTML / Markdown 输出中正确处理。
- 上传结果与图库的单张卡片均左对齐；1920 px 宽屏每行四张，第五张换行后仍左对齐。长图弹窗完整等比居中，Escape 关闭并归还焦点。
- 原图地址依旧返回相同图片字节；Chrome 原生图片页恢复水平居中。只在受限光栅图片响应中允许内联样式，保留 sandbox 和默认禁止加载策略。
- 注册开关、存储子目录保存并读回。
- 390 / 768 / 1440 / 1920 / 2560 px 上传页与图片管理页均无横向溢出，上传区域高度不少于 280 px。
- 系统设置表单与主体等宽居中，手机深色设置及长图弹窗无横向溢出。
- 移动端页面宽度未超过视口；未发现未捕获的浏览器 JavaScript 错误。
- 已生成上传页与图库（浅色 / 深色 / 手机）、单图、居中预览、系统设置截图；已查看桌面上传页、桌面图库和手机深色上传页。本轮另外查看了复制提示与管理员全站图库截图。此前已检查初始化与用户确认弹窗。

## 尚未覆盖

- Docker 镜像构建、群晖 / 飞牛 / ARM 实机。
- 公网反向代理端到端配置和长期负载测试。
- 各类损坏图片的完整解码检测、超大像素图片、EXIF 清理及 WebP 压缩。
- 全功能安全审计。上述测试通过不意味着安全边界可以省略，详见 README。

